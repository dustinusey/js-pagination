import data from"./data.js";const hamMenu=document.getElementById("hamMenu"),nav=document.getElementById("nav");hamMenu.addEventListener("click",(e=>{nav.classList.toggle("show"),hamMenu.classList.toggle("active")}));const pagination=document.getElementById("pagination"),totalItems=data.length,itemsPerPage=9;let pageCount=Math.ceil(totalItems/9),currentPage=1;function handlePagination(e){pagination.innerHTML="";for(let a=1;a<=e;a++)pagination.innerHTML+=`<li class="page-item ${a===currentPage?"active":""}">${a}</li>`}function handleUsers(e,a){employeeList.innerHTML="";const n=9*(currentPage-1),t=n+9,s=e.slice(n,t);s.forEach((e=>{employeeList.innerHTML+=`<li class="user-card">\n            <img src="${e.picture.thumbnail}" alt="">\n            <span class="name">${e.name.first} ${e.name.last}</span>\n            <div class="user-info">\n                <span>${e.email}</span>\n                <span>${e.registered.date}</span>\n            </div>\n        </li>`})),0===s.length&&(employeeList.innerHTML=`<li>There are no employees by the name <span class="no-employees">${a}</span></li>`)}function handlePageChange(e){e.target.classList.contains("page-item")&&(currentPage=Number(e.target.innerText),handlePagination(pageCount),handleUsers(data))}pagination.addEventListener("click",handlePageChange),handlePagination(pageCount),handleUsers(data);const searchContainer=document.getElementById("searchContainer");function createSearch(){let e=document.createElement("i");e.classList="fa-solid fa-magnifying-glass",searchContainer.appendChild(e);let a=document.createElement("input");a.id="userSearch",a.placeholder="Search...",searchContainer.appendChild(a)}createSearch();const userSearch=document.getElementById("userSearch");userSearch.addEventListener("keyup",(e=>{currentPage=1;const a=e.target.value.toLowerCase(),n=data.filter((e=>e.name.first.toLowerCase().includes(a)||e.name.last.toLowerCase().includes(a)));pageCount=Math.ceil(n.length/9),handleUsers(n,a),handlePagination(pageCount)}));